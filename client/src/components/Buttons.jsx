import React from 'react';

const Buttons = (props) => {
    return (
        <div>
            <button class="buttons" id="button-left">Left</button>
            <button class="buttons" id="button-right">Right</button>
        </div>
    )
}

export default Buttons;